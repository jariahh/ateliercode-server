apiVersion: v2
name: ateliercode-server
description: AtelierCode WebRTC signaling server for multi-machine platform
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "16.2.1"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
